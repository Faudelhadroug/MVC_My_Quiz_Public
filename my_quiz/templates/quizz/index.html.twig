{% extends 'base.html.twig' %}

{% block title %}Quizz time!{% endblock %}

{% block body %}
{% include 'header.html.twig'%}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="float-right"> 
        <a href='/quizz/create'><button type="button" class="btn btn-success btn-lg">Create a quizz</button></a>
    </div>
    <h1>All Quizz avaible</h1>
    <ul>
    {% for categorie in categories %}
        <li> <a href="/quizz/{{ id_categories[loop.index0] }}">{{ categories[loop.index0] }}</a></li>
    {% endfor %}
    </ul>
</div>
{% endblock %}
