{% extends 'base.html.twig' %}

{% block title %}Update address email{% endblock %}

{% block body %}
{% include 'header.html.twig'%}
    <h1>Update address email</h1>

    {{ form_start(updateEmailForm) }}
        {{ form_row(updateEmailForm.email) }}
        <button type="submit" class="btn btn-success">Update your email</button>
    {{ form_end(updateEmailForm) }}
{% endblock %}

